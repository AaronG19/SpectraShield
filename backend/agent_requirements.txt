# Agent Security EDR Agent - Dependencies
# Install: pip install -r agent_requirements.txt

# Core
psutil>=5.9.0
requests>=2.31.0
pyyaml>=6.0.1

# File monitoring (optional - for real-time file change detection)
watchdog>=3.0.0

# Windows service support (Windows only)
pywin32>=305; sys_platform == 'win32'

# PyInstaller (optional - for packaging)
# pyinstaller>=6.3.0

yara-python>=4.5.0

